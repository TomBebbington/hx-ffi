import ffi.lib.EasyLibrary;
import ffi.lib.Library;
import ffi.Cif;
import ffi.Function;
import ffi.Pointer;
import ffi.Status;
import ffi.Type;
import ffi.Util;
class AllClasses {
	static function main() {

	}
}