import ffi.lib.EasyLibrary;
import ffi.lib.Library;
import ffi.CallInterface;
import ffi.Function;
import ffi.Pointer;
import ffi.Status;
import ffi.Type;
class AllClasses {
	static function main() {

	}
}